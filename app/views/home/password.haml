- title 'Cambio de clave'
%h2 Cambiar Password
= form_tag password_path do
  - if @user.errors.any?
    #errorExplanation
      %h2 Corriga los siguientes errores por favor
      %ul
        - @user.errors.full_messages.each do |msg|
          %li= msg
  %table.sglea
    %tr
      %td.tit= label :user, :password
      %td.inp= password_field_tag :password
    %tr
      %td.tit= label :user, :password_confirmation
      %td.inp= password_field_tag :password_confirmation
    %tr
      %td
      %td= submit_tag 'Cambiar Clave'
