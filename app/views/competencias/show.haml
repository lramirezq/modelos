- title "INFORME DE TRABAJO "
%h2 INFORME DE TRABAJO N° #{@competencia.id.to_s}

= form_for @competencia, :builder => DisabledFormBuilder do |f|
  = render 'form', :f => f, :form_disabled => true


= link_button  "Enviar Datos a Contabilidad", envio_mail_path(@competencia)