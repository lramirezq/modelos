- title "Modificación de Agencia Madre"

- form_for @agenciamadre, :html => { :multipart => true } do |f|
  = render 'form', :f => f, :form_disabled => false
  .actions{:align => "center"}
    = f.submit 'Modificar Agencia Madre'
